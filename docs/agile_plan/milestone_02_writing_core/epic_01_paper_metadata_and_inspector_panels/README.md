# Epic 01 - Paper Metadata and Inspector Panels

Source phase: Phase 4

Objective: make paper metadata editable through structured forms that drive the ghost-page output.

Tracking rule: all tasks in this epic must be marked `Done` before the epic is considered complete.

Epic status:

- [x] Done

Verification note:

- Verified against the current implementation on 2026-03-08. The epic is represented by the extracted inspector UI in `src/renderer/app/inspector/PaperInspectorPanel.tsx`, renderer coverage in `tests/renderer/app-shell.test.tsx`, domain validation in `tests/unit/domain/paper-metadata.test.ts`, and persistence coverage in `tests/integration/persistence/persistence-context.test.ts`.
