# macOS code signing and notarization
# Set these environment variables before running `npm run make` for signed builds.
# Without these, builds are unsigned (suitable for local testing).
APPLE_ID="your-apple-id@example.com"
APPLE_PASSWORD="xxxx-xxxx-xxxx-xxxx"
APPLE_TEAM_ID="TEAMID"
